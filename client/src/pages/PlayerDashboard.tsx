import DashboardLayout from "@/components/DashboardLayout";
import { BracketLegend } from "@/components/BracketView";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { trpc } from "@/lib/trpc";
import { ArrowUpRight, CalendarDays, ChevronRight, Crosshair, Flame, MapPin, Radio, Shield, Sparkles, Trophy, Users } from "lucide-react";
import { Link } from "wouter";

function formatDate(value: Date | string | null | undefined) {
  if (!value) return "TBD";
  return new Date(value).toLocaleDateString(undefined, { month: "short", day: "numeric" });
}

export default function PlayerDashboard() {
  const { data, isLoading } = trpc.dashboard.player.useQuery();
  const profile = data?.profile;
  const winRate = profile ? Math.round((profile.wins / Math.max(profile.wins + profile.losses, 1)) * 100) : 0;

  return <DashboardLayout><div className="relative overflow-hidden px-5 py-8 text-white sm:px-8 lg:px-12"><div className="pointer-events-none absolute -right-32 -top-40 h-[32rem] w-[32rem] rounded-full bg-lime-300/[0.07] blur-3xl" /><div className="relative mx-auto max-w-[1400px] space-y-8"><header className="flex flex-col justify-between gap-5 border-b border-white/[0.08] pb-7 lg:flex-row lg:items-end"><div><div className="mb-3 flex items-center gap-2 text-[10px] font-bold uppercase tracking-[0.24em] text-lime-300"><span className="h-1.5 w-1.5 animate-pulse rounded-full bg-lime-300" />Live operations / player 01</div><h1 className="text-4xl font-semibold tracking-[-0.05em] sm:text-5xl">Good to see you, <span className="text-lime-300">{profile?.handle ?? "competitor"}.</span></h1><p className="mt-3 max-w-xl text-sm leading-6 text-white/45">The circuit is moving. Here is your current position across teams, matches, and the next window to make noise.</p></div><div className="flex gap-2"><Button variant="outline" className="border-white/10 bg-white/[0.04] text-white hover:bg-white/[0.08]">Export profile</Button><Link href="/dashboard/team"><Button className="bg-lime-300 text-black hover:bg-lime-200">Manage team <ArrowUpRight className="ml-2 h-4 w-4" /></Button></Link></div></header>

<div className="grid gap-4 lg:grid-cols-[1.45fr_0.55fr_0.55fr_0.8fr]"><Card className="overflow-hidden border-lime-300/20 bg-[linear-gradient(135deg,rgba(190,242,100,0.13),rgba(255,255,255,0.03)_55%)]"><CardContent className="relative flex min-h-[190px] flex-col justify-between p-6"><div className="absolute right-0 top-0 h-40 w-40 rounded-full bg-lime-300/10 blur-3xl" /><div className="relative flex items-start justify-between"><div className="flex items-center gap-4"><div className="grid h-14 w-14 place-items-center rounded-2xl border border-lime-300/30 bg-lime-300/15 text-xl font-black text-lime-200">{profile?.handle?.slice(0, 2) ?? "NA"}</div><div><p className="text-xl font-semibold">{profile?.handle ?? "New player"}</p><p className="mt-1 flex items-center gap-2 text-xs text-white/45"><MapPin className="h-3.5 w-3.5" />{profile?.region ?? "SEA"} <span className="text-white/20">/</span> {profile?.primaryGame ?? "VALORANT"}</p></div></div><Badge className="border-lime-300/20 bg-lime-300/10 text-lime-200">SYNCED</Badge></div><div className="relative grid grid-cols-3 gap-4 border-t border-white/10 pt-4"><div><p className="text-2xl font-semibold">{profile?.wins ?? 0}</p><p className="mt-1 text-[10px] uppercase tracking-[0.16em] text-white/35">Wins</p></div><div><p className="text-2xl font-semibold">{profile?.losses ?? 0}</p><p className="mt-1 text-[10px] uppercase tracking-[0.16em] text-white/35">Losses</p></div><div><p className="text-2xl font-semibold text-lime-300">{winRate}%</p><p className="mt-1 text-[10px] uppercase tracking-[0.16em] text-white/35">Win rate</p></div></div></CardContent></Card><StatCard icon={<Flame className="h-4 w-4" />} label="Form streak" value="W4" meta="+12 rating" accent="orange" /><StatCard icon={<Trophy className="h-4 w-4" />} label="Circuit rank" value="#084" meta="Top 8% SEA" accent="lime" /><StatCard icon={<CalendarDays className="h-4 w-4" />} label="Next check-in" value="02:14:36" meta="Meno Arena Clash" accent="blue" /></div>

<div className="grid gap-6 xl:grid-cols-[1.35fr_0.65fr]"><Card className="border-white/[0.08] bg-white/[0.025]"><CardHeader className="flex flex-row items-center justify-between border-b border-white/[0.07] px-6 py-5"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-lime-300">Your next windows</p><CardTitle className="mt-2 text-xl text-white">Match schedule</CardTitle></div><Link href="/brackets/102"><Button variant="ghost" className="text-xs text-white/50 hover:bg-white/[0.05] hover:text-white">Open bracket <ChevronRight className="ml-1 h-3.5 w-3.5" /></Button></Link></CardHeader><CardContent className="p-0">{isLoading ? <div className="p-6 text-sm text-white/40">Syncing your match room…</div> : data?.matches?.slice(0, 4).map((match, index) => <div key={match.id} className="flex items-center gap-4 border-b border-white/[0.06] px-6 py-4 last:border-0"><div className="w-14 text-center"><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-white/30">{match.status === "live" ? "Now" : formatDate(match.scheduledAt)}</p><p className="mt-1 font-mono text-xs text-white/60">{match.status === "live" ? "LIVE" : `${18 + index}:30`}</p></div><div className="h-9 w-px bg-white/10" /><div className="grid h-9 w-9 place-items-center rounded-xl bg-white/[0.06] text-xs font-black text-lime-200">{index === 0 ? <Radio className="h-4 w-4" /> : "QF"}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold text-white/85">{index === 0 ? "Null Sector" : index === 1 ? "Redline vs Vanta" : "Neon Circuit Qualifier"}</p><p className="mt-1 text-xs text-white/35">{match.status === "live" ? "Map 2 · Haven" : "Single elimination · Round {match.round}"}</p></div><Badge className={match.status === "live" ? "border-lime-300/20 bg-lime-300/10 text-lime-300" : "border-white/10 bg-white/[0.04] text-white/45"}>{match.status}</Badge></div>)}</CardContent></Card><div className="space-y-6"><Card className="border-white/[0.08] bg-white/[0.025]"><CardHeader className="px-6 pb-3 pt-5"><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-white/30">Active roster</p><CardTitle className="mt-2 text-xl text-white">{data?.teams?.[0]?.name ?? "No team yet"}</CardTitle></CardHeader><CardContent className="px-6 pb-6"><div className="flex items-center justify-between"><div className="flex -space-x-2">{["N", "K", "R", "+2"].map((item, index) => <div key={item} className={`grid h-9 w-9 place-items-center rounded-full border-2 border-[#111611] text-[10px] font-bold ${index === 0 ? "bg-lime-300 text-black" : "bg-white/10 text-white/55"}`}>{item}</div>)}</div><Link href="/dashboard/team"><Button variant="outline" size="sm" className="border-white/10 bg-transparent text-xs text-white/55 hover:bg-white/[0.06]">View roster</Button></Link></div><div className="mt-5 flex items-center gap-2 text-xs text-white/40"><Shield className="h-3.5 w-3.5 text-lime-300" />{data?.clan ? `${data.clan.tag} · ${data.clan.name}` : "Independent roster"}</div></CardContent></Card><Card className="border-lime-300/15 bg-lime-300/[0.06]"><CardContent className="p-6"><div className="flex items-center gap-2 text-lime-300"><Sparkles className="h-4 w-4" /><span className="text-[10px] font-bold uppercase tracking-[0.18em]">AI match intel</span></div><p className="mt-4 text-sm leading-6 text-white/70">Your last four maps show a 24% improvement in opening-duel conversion. Keep pressure on early site control.</p><div className="mt-5 flex items-center justify-between border-t border-lime-300/15 pt-4 text-xs"><span className="text-white/35">Updated 4m ago</span><Button variant="ghost" size="sm" className="px-0 text-lime-300 hover:bg-transparent hover:text-lime-200">View report <ChevronRight className="ml-1 h-3.5 w-3.5" /></Button></div></CardContent></Card></div></div>

<Card className="border-white/[0.08] bg-white/[0.025]"><CardHeader className="flex flex-col gap-3 px-6 py-5 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-white/30">Current event pulse</p><CardTitle className="mt-2 text-xl text-white">Tournament trajectory</CardTitle></div><BracketLegend /></CardHeader><CardContent className="grid gap-4 px-6 pb-6 sm:grid-cols-3">{(data?.tournaments ?? []).slice(0, 3).map((tournament, index) => <Link href={`/brackets/${tournament.id}`} key={tournament.id}><div className="group rounded-2xl border border-white/[0.08] bg-black/10 p-4 transition-colors hover:border-lime-300/30"><div className="flex items-center justify-between"><Badge className="border-white/10 bg-white/[0.05] text-[10px] text-white/45">{tournament.game}</Badge><ArrowUpRight className="h-4 w-4 text-white/20 transition-colors group-hover:text-lime-300" /></div><p className="mt-5 text-sm font-semibold text-white/85">{tournament.name}</p><div className="mt-4 flex items-center justify-between text-xs text-white/35"><span>{tournament.status}</span><span>{(tournament.prizePoolCents / 100).toLocaleString(undefined, { style: "currency", currency: "USD", maximumFractionDigits: 0 })}</span></div></div></Link>)}</CardContent></Card></div></div></DashboardLayout>;
}

function StatCard({ icon, label, value, meta, accent }: { icon: React.ReactNode; label: string; value: string; meta: string; accent: "lime" | "orange" | "blue" }) {
  const color = accent === "lime" ? "text-lime-300 bg-lime-300/10" : accent === "orange" ? "text-orange-300 bg-orange-300/10" : "text-sky-300 bg-sky-300/10";
  return <Card className="border-white/[0.08] bg-white/[0.025]"><CardContent className="min-h-[190px] p-6"><div className={`grid h-9 w-9 place-items-center rounded-xl ${color}`}>{icon}</div><p className="mt-8 text-[10px] font-bold uppercase tracking-[0.16em] text-white/30">{label}</p><p className="mt-2 text-3xl font-semibold tracking-tight text-white">{value}</p><p className="mt-1 text-xs text-white/35">{meta}</p></CardContent></Card>;
}
