import DashboardLayout from "@/components/DashboardLayout";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { trpc } from "@/lib/trpc";
import { ArrowLeft, CalendarDays, Coins, Radio, ShieldCheck, Trophy } from "lucide-react";
import { useState } from "react";
import { Link } from "wouter";
import { toast } from "sonner";

const initialForm = { name: "", game: "VALORANT", format: "single_elimination" as const, startsAt: "", registrationClosesAt: "", prizePool: "0", entryFee: "0", maxTeams: "16", rules: "", sponsorName: "", streamUrl: "", clanEligible: true };

export default function TournamentCreatePage() {
  const [form, setForm] = useState(initialForm);
  const createTournament = trpc.tournaments.create.useMutation({
    onSuccess: tournament => toast.success("Tournament created", { description: `${tournament.name} is ready for registration.` }),
    onError: error => toast.error("Tournament creation failed", { description: error.message }),
  });
  const update = (key: keyof typeof form, value: string | boolean) => setForm(current => ({ ...current, [key]: value }));

  return <DashboardLayout><div className="min-h-screen px-5 py-8 text-white sm:px-8 lg:px-12"><div className="mx-auto max-w-[1120px] space-y-8"><header className="border-b border-white/[0.08] pb-7"><Link href="/dashboard/player"><Button variant="ghost" className="mb-5 -ml-3 gap-2 px-3 text-xs text-white/40 hover:bg-white/[0.05] hover:text-white"><ArrowLeft className="h-3.5 w-3.5" />Back to overview</Button></Link><p className="text-[10px] font-bold uppercase tracking-[0.24em] text-lime-300">Organizer console / tournament creation</p><h1 className="mt-3 text-4xl font-semibold tracking-[-0.05em]">Open the next <span className="text-lime-300">high-stakes room.</span></h1><p className="mt-3 max-w-xl text-sm leading-6 text-white/45">Publish the rules, economics, schedule, and broadcast signal that make a tournament feel real before round one starts.</p></header>

<div className="grid gap-5 lg:grid-cols-[1fr_0.42fr]"><Card className="border-white/[0.08] bg-white/[0.025]"><CardHeader className="border-b border-white/[0.07] px-6 py-5"><div className="flex items-center gap-3"><Trophy className="h-5 w-5 text-lime-300" /><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-lime-300">Tournament brief</p><CardTitle className="mt-2 text-xl">Set the competitive contract</CardTitle></div></div></CardHeader><CardContent><form className="space-y-5" onSubmit={event => { event.preventDefault(); createTournament.mutate({ name: form.name, game: form.game, format: form.format, startsAt: new Date(form.startsAt), registrationClosesAt: form.registrationClosesAt ? new Date(form.registrationClosesAt) : undefined, prizePoolCents: Number(form.prizePool) * 100, entryFeeCents: Number(form.entryFee) * 100, maxTeams: Number(form.maxTeams), rules: form.rules || undefined, sponsorName: form.sponsorName || undefined, streamUrl: form.streamUrl, clanEligible: form.clanEligible }); }}><div className="grid gap-4 sm:grid-cols-[1fr_180px]"><div><Label className="text-white/55">Tournament name</Label><Input required value={form.name} onChange={event => update("name", event.target.value)} className="mt-2 border-white/10 bg-white/[0.04] text-white" placeholder="Meno Open // Season 01" /></div><div><Label className="text-white/55">Game</Label><Input required value={form.game} onChange={event => update("game", event.target.value)} className="mt-2 border-white/10 bg-white/[0.04] text-white" /></div></div><div className="grid gap-4 sm:grid-cols-2"><div><Label className="text-white/55">Format</Label><select value={form.format} onChange={event => update("format", event.target.value)} className="mt-2 h-10 w-full rounded-md border border-white/10 bg-white/[0.04] px-3 text-sm text-white"><option value="single_elimination">Single elimination</option><option value="double_elimination">Double elimination</option><option value="round_robin">Round robin</option><option value="swiss">Swiss</option></select></div><div><Label className="text-white/55">Max teams</Label><Input required type="number" min="2" value={form.maxTeams} onChange={event => update("maxTeams", event.target.value)} className="mt-2 border-white/10 bg-white/[0.04] text-white" /></div></div><div className="grid gap-4 sm:grid-cols-2"><div><Label className="text-white/55">Starts at</Label><Input required type="datetime-local" value={form.startsAt} onChange={event => update("startsAt", event.target.value)} className="mt-2 border-white/10 bg-white/[0.04] text-white" /></div><div><Label className="text-white/55">Registration closes</Label><Input type="datetime-local" value={form.registrationClosesAt} onChange={event => update("registrationClosesAt", event.target.value)} className="mt-2 border-white/10 bg-white/[0.04] text-white" /></div></div><div className="grid gap-4 sm:grid-cols-2"><div><Label className="text-white/55">Prize pool (USD)</Label><Input type="number" min="0" value={form.prizePool} onChange={event => update("prizePool", event.target.value)} className="mt-2 border-white/10 bg-white/[0.04] text-white" /></div><div><Label className="text-white/55">Entry fee (USD)</Label><Input type="number" min="0" value={form.entryFee} onChange={event => update("entryFee", event.target.value)} className="mt-2 border-white/10 bg-white/[0.04] text-white" /></div></div><div><Label className="text-white/55">Rules and briefing</Label><Textarea value={form.rules} onChange={event => update("rules", event.target.value)} className="mt-2 min-h-28 border-white/10 bg-white/[0.04] text-white" placeholder="Map pool, check-in, match windows, and disputes…" /></div><div className="grid gap-4 sm:grid-cols-2"><div><Label className="text-white/55">Sponsor name</Label><Input value={form.sponsorName} onChange={event => update("sponsorName", event.target.value)} className="mt-2 border-white/10 bg-white/[0.04] text-white" placeholder="Optional" /></div><div><Label className="text-white/55">Stream URL</Label><Input value={form.streamUrl} onChange={event => update("streamUrl", event.target.value)} className="mt-2 border-white/10 bg-white/[0.04] text-white" placeholder="https://twitch.tv/..." /></div></div><label className="flex cursor-pointer items-center gap-3 rounded-xl border border-white/[0.08] bg-white/[0.02] p-4 text-sm text-white/65"><input type="checkbox" checked={form.clanEligible} onChange={event => update("clanEligible", event.target.checked)} className="accent-lime-300" />Allow verified clan organizations to register</label><Button disabled={createTournament.isPending} type="submit" className="w-full bg-lime-300 text-black hover:bg-lime-200">{createTournament.isPending ? "Publishing…" : "Publish tournament brief"}</Button></form></CardContent></Card>

<Card className="h-fit border-white/[0.08] bg-white/[0.025]"><CardHeader className="px-6 pb-3 pt-6"><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-white/30">Publish checklist</p><CardTitle className="mt-2 text-xl">Make it legible</CardTitle></CardHeader><CardContent className="space-y-3 px-6 pb-6"><div className="flex gap-3 rounded-xl border border-white/[0.07] bg-white/[0.02] p-3 text-sm text-white/55"><CalendarDays className="mt-0.5 h-4 w-4 shrink-0 text-lime-300" />Lock the schedule before teams register.</div><div className="flex gap-3 rounded-xl border border-white/[0.07] bg-white/[0.02] p-3 text-sm text-white/55"><Coins className="mt-0.5 h-4 w-4 shrink-0 text-lime-300" />Prize and entry economics are stored as cents.</div><div className="flex gap-3 rounded-xl border border-white/[0.07] bg-white/[0.02] p-3 text-sm text-white/55"><Radio className="mt-0.5 h-4 w-4 shrink-0 text-lime-300" />A stream URL makes the broadcast signal discoverable.</div><Badge className="mt-2 border-lime-300/20 bg-lime-300/10 text-lime-200"><ShieldCheck className="mr-1.5 h-3 w-3" />Organizer-only action</Badge></CardContent></Card></div></div></div></DashboardLayout>;
}
